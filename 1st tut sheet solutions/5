#include <stdio.h>
int main(){
  int nr, dr;
  printf("Enter Numerator: ");
  scanf("%d", &nr);
  printf("Enter Denominator: ");
  scanf("%d", &dr);
  printf("The fraction is %d/%d", nr, dr);
  return 0;
}
